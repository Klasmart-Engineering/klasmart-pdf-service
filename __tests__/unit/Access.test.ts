
describe('Access', () => {
    describe('Authorized', () => {

        describe('should always set response.locals.authChecked to true', () => {
            const inputs = [];
        });

        describe('should call next and return undefined for any Authorization when types includes any', () => {

        });

        describe ('should call next and return undefined when types includes set authType', () => {

        });

        describe('should error when authType is defined but not included in types', async () => {

        });

        it('should throw error with status 401 when an error is thrown and authType is Anonymous', () => {

        });

        it('should throw error with status 401 when an erorr is thrown and authType is not Anonymous', () => {

        });
    });
});